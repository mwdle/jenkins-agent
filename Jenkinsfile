@Library("JenkinsPipelines") _ // See https://github.com/mwdle/JenkinsPipelines

dockerImagePipeline(defaultImageName: 'mwdle/jenkins-agent') // This specific pipeline is dependent on the Docker Workflow Jenkins plugin